# Env variables to put in .env.local (not committed to git)
PORT=8081
NODE_ENV=development

# Postgres (local hors Docker)
DB_URL=postgres://dzaster_user:dzaster_password@localhost:5433/dzaster_db

# Secrets applicatifs (remplacer en prod)
AUTH_SERVICE_TOKEN=2hfds524hgrt6es
JWT_SECRET=bvcbtezfds485cxv1
JWT_EXPIRES_IN=1h